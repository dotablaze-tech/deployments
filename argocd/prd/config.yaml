apps:
  # --- PRD environment ---
  - name: secret-store-prd
    namespace: dotablaze-tech-prd
    chartPath: charts/secret-store
    syncWave: "0"
    valueFiles:
      - values.yaml
      - values-prd.yaml

  - name: meowbot-prd
    namespace: dotablaze-tech-prd
    chartPath: charts/meowbot
    syncWave: "1"
    valueFiles:
      - values.yaml
      - values-prd.yaml
